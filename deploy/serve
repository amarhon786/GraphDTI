#!/usr/bin/env bash
# SageMaker container entry point. Launches the FastAPI app on port 8080.
set -euo pipefail
exec python -m graphdti.serving.app \
    --ckpt "${GRAPHDTI_CKPT:-/opt/ml/model/dti.pt}" \
    --host "${GRAPHDTI_HOST:-0.0.0.0}" \
    --port "${GRAPHDTI_PORT:-8080}"
